name: asdfasdfafd
on:
  workflow_dispatch:
  push:
    branches:
      - "*"
jobs:
  release:
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/heads/')
    steps:
      - name: Run asfdasdf
        run: echo ${{ github.ref }}
